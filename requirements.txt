# ==============================================================
# Power Grid Monitor - Complete Pinned Dependencies
# All direct AND transitive dependencies are listed here
# so that Elastic Beanstalk does NOT need to run pip install.
# ==============================================================

# --- Direct Dependencies ---
Flask==3.0.3
paho-mqtt==1.6.1
requests==2.32.3
boto3==1.34.144
python-dotenv==1.0.1
gunicorn==22.0.0

# --- Flask transitive dependencies ---
Werkzeug==3.0.1
Jinja2==3.1.6
MarkupSafe==2.1.5
itsdangerous==2.2.0
click==8.3.1
blinker==1.9.0

# --- requests transitive dependencies ---
urllib3==2.6.3
certifi==2024.2.2
charset-normalizer==3.3.2
idna==3.6

# --- boto3 / botocore transitive dependencies ---
botocore==1.34.162
jmespath==1.1.0
s3transfer==0.10.4
python-dateutil==2.9.0.post0
six==1.17.0

# --- packaging (used by various packages) ---
packaging==26.0

# --- Chart.js & Frontend Instructions ---
# The dashboard frontend loads Chart.js dynamically from a CDN.
# No node_modules or package.json configurations are required for the web interface.
# The CDN link used in templates/index.html is:
# https://cdn.jsdelivr.net/npm/chart.js
# This ensures a lightweight, vanilla HTML/CSS/JavaScript client-side implementation.
